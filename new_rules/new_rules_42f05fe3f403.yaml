규칙 1:

rules:
  - id: python_django_security_injection_request-data-write_request-data-write
    pattern: |
      with open('temp_code.py', 'w') as f:
        f.write(...)
    message: "코드 인젝션 취약점: 사용자 입력을 직접 파일에 쓰기"
    languages: [python]
    severity: ERROR

규칙 2:

rules:
  - id: python_flask_security_audit_debug-enabled_debug-enabled
    pattern: "app.run(debug=True)"
    message: "디버그 모드 취약점: Flask 앱이 디버그 모드에서 실행되는 것은 보안 위험을 초래합니다."
    languages: [python]
    severity: ERROR